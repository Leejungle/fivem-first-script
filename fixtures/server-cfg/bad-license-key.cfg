# BAD CONFIG — invalid license key
# What is wrong: sv_licenseKey is set to the string "INVALID", which does not
# match the expected format for a Cfx.re license key. fxpreflight should fire
# R002 CRITICAL against this file.

# Network binding
endpoint_add_tcp "0.0.0.0:30120"
endpoint_add_udp "0.0.0.0:30120"

# Server identity
sv_hostname "fxpreflight Test Server"
sv_maxclients 32

# License key — deliberately invalid to trigger R002
sv_licenseKey "INVALID"

# Resources
ensure mapmanager
ensure chat
ensure spawnmanager
